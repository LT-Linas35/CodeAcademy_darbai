---
name: Patikrinam
on:
  workflow_dispatch:
jobs:
 build:
  name: Lintinam
   runs-on: node
   steps:
    - name: Irasome tslint ir eslint
      run: npm install --save-dev eslint globals @eslint/js tslint
    - name: Pradedam testus
      run: npm run test && npm run lint_es && npm run lint_ts
